define host {
    use generic-host
    check_command check-host-alive
    max_check_attempts 5
    notification_interval 30
    host_name {{ host.fqdn }}
    alias {{ host.fqdn }}
    address {{ host.address }}
}

{% for file in monitoring_class_files %}
{% include file %}
{% endfor %}
